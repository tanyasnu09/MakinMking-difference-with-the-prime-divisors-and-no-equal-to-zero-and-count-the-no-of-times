#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
  long   int n;
    scanf("%ld",&n);
    for (int i=2; i<=n ;i++)
    {
        if(n%i==0)
        {
           

        
        int sum=n; 
        int count=0;
        do
        {
           sum=sum-i;
           count++;
    
        
         
        }
        while(sum!=0);
        printf("%d",count);
      break;
        }
    

        }
    /* Enter your code . Read input from STDIN. Print output to STDOUT */    

}
